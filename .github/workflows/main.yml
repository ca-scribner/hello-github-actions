name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: "Mona"
      - run: echo "HIIIIIII.  Do I show up when github actions fire on my PR?  Or do I only show up once I'm in master"
